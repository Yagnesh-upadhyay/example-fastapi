name: Build and Deploy Code

# on:
#   push:
#     branches: #["main"]
#       - "main"
#       - "another_branch"
#   pull_request:
#     branches:
#       - "test_branch"

on: [push, pull_request]

jobs:
  job1:
    env:
      SECRETKEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
      ALGORITHM=HS256
      ACCESS_TOKEN_EXPIRE_MINUTES=30
      DATABASE_URL=postgresql://postgres:password123@localhost:5432/fastapi
    runs-on: ubuntu-latest
    steps: 
      - name: pulling git repo
        uses: actions/checkout@v7
      # - name: say hi to yagnesh
      #   run: echo "hello yagnesh"
      - name: Install python version 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: update pip
        run: python -m pip install --upgrade pip
      - name: install all dependencies
        run: pip install -r requirements.txt
      - name: test with pytest
        run: |
          pip install pytest
          pytest

